package de.cubeisland.engine.command.parameter;

public abstract class ParameterBase implements Parameter
{
    /*
    Static Reader ? replace them with named param with no consuming / caution when parsing we'll need to map to alias name not actual name!

    class type
    class reader
    consuming (greed)
    required?
    valuelabel
    description
    completer ?
     */
}
